---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"a"> {
    isExternal?: boolean;
}

const { href, isExternal = false, style, ...attrs } = Astro.props as Props;
---

<a href={href} rel={isExternal ? "external" : "prefetch"} {...attrs}>
    <slot />
</a>
